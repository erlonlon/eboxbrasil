 <div class="5grid-layout">
      <div class="row">
        <div id="banner" class="12u">
          <div class="container">          

          	 <div id="slider" class="nivoSlider">
          	 	<% @category_galeries.galeries.limit(2).each do |g| %>
          	 	 <%= image_tag(g.document.url(:small), :size => "1150x420") %>
            
              <% end %>
		    </div>
        <!--close slider-->

          </div>
        </div>
      </div>
    </div>